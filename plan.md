# TODO.app 全栈学习项目 · 计划书

> 目标：在 **2 个月（约 8 周）** 内，从零独立完成一个可上线、可演示的全栈 TODO 应用。
> 技术栈：**Vue 3（前端）+ Flask（后端）+ MySQL（数据库）**，并贯穿学习 Git、网络、部署运维等知识。
>
> 学习方式：**每次只完成一个章节的一小部分**，边学边写，不一次性堆代码。

---

## 一、总览：项目的最终形态

一个用户可以使用的 TODO 应用，具备以下能力：

1. **注册 / 登录 / 退出**（知道"数据属于谁"）
2. **增、删、改、查（CRUD）任务**（核心功能）
3. **任务状态管理**（未完成 / 已完成）
4. **任务分类与筛选**（按标签、按状态、按关键词搜索）
5. **响应式界面**（浏览器、手机都能用）
6. **部署上线**（有一个真实的网址，别人能访问）

整个项目会经历 **"先跑起来 → 再变完整 → 最后变专业"** 三个阶段。

---

## 二、项目结构（最终会变成这样）

```
todo-app/
├── frontend/          # Vue 前端
│   ├── src/
│   └── ...
├── backend/           # Flask 后端
│   ├── app.py
│   ├── models.py
│   └── ...
├── docs/              # 学习笔记、接口文档
├── plan.md            # 本计划书
└── README.md          # 项目说明
```

> 这个结构不是一开始就建好的，会随着章节逐步长出来。前几章可能只是几个散落的文件，后面再整理成规范结构。

---

## 三、章节计划（按学习顺序）

> 每章包含：**学习目标**、**要做的内容**、**交付物**（判断这一章是否完成的标志）。
> 章与章之间有依赖关系，请按顺序进行，不要跳章。

---

### 阶段零：准备与打地基（第 1 周）

#### 第 1 章：开发环境与工具
**学习目标**：把开发要用到的所有工具装好、配置好，理解每个工具是干嘛的。

- 认识终端（Terminal）和命令行
- 安装并配置：VS Code、Python 3、Node.js、MySQL、Git
- 理解 `PATH` 环境变量、什么是"包管理器"（pip / npm）
- 学会用 VS Code 打开文件夹、运行命令

**交付物**：一条命令能分别打印出 `python --version`、`node --version`、`git --version` 的版本号，MySQL 能成功登录。

#### 第 2 章：Git 基础与版本管理（上）
**学习目标**：理解为什么要做版本管理，掌握最核心的 Git 命令。

- 什么是版本管理、什么是仓库（repository）
- `git init`、`git add`、`git commit`、`git status`、`git log`
- 写好第一条 commit message
- 认识 `.gitignore`（哪些文件不该提交）

**交付物**：本地有一个 Git 仓库，至少做了 3 次有意义的提交，能解释每次提交改了什么。

#### 第 3 章：HTML / CSS / JavaScript 速通
**学习目标**：掌握前端三大件的基础，为 Vue 做铺垫（Vue 建立在它们之上）。

- HTML：标签、结构、语义
- CSS：选择器、盒模型、Flex 布局、常用的样式属性
- JavaScript：变量、函数、数组、对象、DOM 操作、`fetch` 发请求
- 网络预热：浏览器是怎么向服务器要数据的（HTTP 请求 / 响应）

**交付物**：用纯 HTML/CSS/JS 做一个静态的 TODO 页面（数据存在内存里，刷新就没了），能添加和删除任务。

---

### 阶段一：前后端各自跑通（第 2–3 周）

#### 第 4 章：Vue 3 入门
**学习目标**：理解组件化思想，用 Vue 重写第 3 章的静态页面。

- 用 Vite 创建第一个 Vue 项目，理解项目结构
- 响应式数据（`ref` / `reactive`）、模板语法、指令（`v-model`、`v-for`、`v-if`）
- 事件处理、组件拆分
- 单文件组件（`.vue`）

**交付物**：Vue 版 TODO，功能与第 3 章一致，但代码是组件化的，数据用响应式状态管理。

#### 第 5 章：Flask 入门与你的第一个接口
**学习目标**：理解"后端"是什么，写出第一个能返回数据的接口。

- Python 虚拟环境（venv）与依赖管理（`requirements.txt`）
- Flask 路由（`@app.route`）、启动服务
- HTTP 方法：GET / POST / PUT / DELETE
- 返回 JSON 数据、用 Postman 或浏览器测试接口
- 理解什么是 API、什么是 RESTful 风格

**交付物**：一个 Flask 服务能启动，访问某个 URL 能返回一串 JSON 数据。

#### 第 6 章：MySQL 入门与数据持久化
**学习目标**：理解数据库的作用，学会用 SQL 存取数据。

- 关系型数据库基本概念（表、行、列、主键、外键）
- SQL 基础：`CREATE`、`INSERT`、`SELECT`、`UPDATE`、`DELETE`
- 用 Python 连接 MySQL（`pymysql` 或 SQLAlchemy）
- 什么是 ORM、为什么要用它

**交付物**：能用代码往 MySQL 里写入一条任务、并读出来。

---

### 阶段二：前后端联调，做出"完整版"（第 4–5 周）

#### 第 7 章：前后端打通（第一个完整闭环）
**学习目标**：把 Vue、Flask、MySQL 串起来，完成真正的"全栈"。

- 前端通过 `fetch` / axios 调用后端接口
- 解决跨域问题（CORS）——理解浏览器的同源策略
- 把任务的增删改查全部接到真实后端和数据库上
- 理解"前端状态"和"后端数据"的区别与同步

**交付物**：TODO 应用的所有增删改查都走真实接口、数据存进 MySQL，刷新页面数据不丢。

#### 第 8 章：用户认证（注册 / 登录 / 退出）
**学习目标**：理解登录是怎么工作的，数据如何区分归属。

- 用户表设计（用户名、密码）
- 密码安全：为什么不能明文存密码（哈希 `hashlib` / `bcrypt`）
- 会话（Session）与 Token（JWT）是什么、区别是什么
- 登录态管理：前端记住"我是谁"、后端校验"你是谁"
- 让每个任务属于某个用户

**交付物**：不同账号登录后看到的是**各自**的任务，互不干扰；未登录会被拦截。

#### 第 9 章：功能增强（分类 / 筛选 / 搜索）
**学习目标**：学会给项目加新功能时"改哪些层"的通用思路。

- 给任务加分类、标签、优先级、截止时间
- 前端做筛选、搜索、排序
- 后端接口支持查询参数（`?status=done` 之类）
- 用户体验打磨：空状态、加载中、错误提示

**交付物**：TODO 应用功能完整，是一个"能日常使用"的合格应用。

---

### 阶段三：专业与上线（第 6–8 周）

#### 第 10 章：Git 进阶与远程协作
**学习目标**：把项目放到 GitHub，掌握多人协作的核心流程。

- 创建 GitHub 仓库、`git remote`、`git push` / `pull`
- 分支（branch）、合并（merge）、Pull Request
- 理解 `.gitignore` 与敏感信息管理（数据库密码不能提交）
- 写好 `README.md`

**交付物**：项目在 GitHub 上完整可克隆，`README` 能让别人看懂并跑起来。

#### 第 11 章：部署上线
**学习目标**：把项目放到真实的服务器上，得到一个别人能访问的网址。

- Linux 基础命令、SSH 远程登录
- 购买 / 使用一台云服务器（如阿里云、腾讯云，或免费的）
- 部署后端（Gunicorn / systemd）、部署前端（Nginx 静态托管）
- 配置域名、HTTPS（证书）
- 环境变量管理（生产环境配置与本地分开）

**交付物**：别人能通过一个公网网址访问你的 TODO 应用，且是 HTTPS。

#### 第 12 章：复盘、优化与扩展
**学习目标**：把一个"能跑"的项目，整理成"拿得出手"的项目。

- 代码整理与重构（命名、注释、目录结构）
- 错误处理、日志
- 性能与安全基础（SQL 注入、XSS、CSRF 等常见问题的概念）
- 写项目总结文档、准备面试 / 作品集介绍
- 可选扩展方向：部署到 Docker、添加数据可视化、移动端适配优化等

**交付物**：一份完整的项目总结 + 一个可演示、可介绍的作品。

---

## 四、时间安排建议

| 阶段 | 章节 | 建议用时 | 核心成果 |
|------|------|----------|----------|
| 阶段零 | 第 1–3 章 | 第 1 周 | 环境就绪，Git 会用，前端三件套入门 |
| 阶段一 | 第 4–6 章 | 第 2–3 周 | Vue、Flask、MySQL 各自跑通 |
| 阶段二 | 第 7–9 章 | 第 4–5 周 | 完整可用的 TODO 应用 |
| 阶段三 | 第 10–12 章 | 第 6–8 周 | 上线 + 专业整理 |

> 时间只是参考。**学得慢一点没关系，重要的是每章都真正理解了再往下走。**

---

## 五、学习建议（重要）

1. **每次只做一件事**：一次会话只推进一个章节的一小步，别贪多。
2. **遇到报错是常态**：报错不是失败，是学习的最佳材料。学会自己读报错信息。
3. **每章都做笔记**：用自己的话记录"这一章我学到了什么"，放在 `docs/` 里。
4. **善用提问**：卡住 15 分钟还没头绪，就来问我，把报错信息贴给我。
5. **坚持提交 Git**：每完成一个小功能就 commit 一次，养成习惯。

---

## 六、待定决策（后续章节会一起确认）

以下问题不急着现在定，到对应章节再决定，这里先记着：

- Vue 使用组合式 API（`<script setup>`）还是选项式 API？（建议：组合式 API）
- 数据库访问用原生 SQL 还是 ORM（SQLAlchemy）？（建议：先用原生理解原理，再上 ORM）
- 登录用 Session 还是 JWT？（建议：先 Session 理解原理，再 JWT）
- 前端是否引入 TypeScript？（建议：本项目先用 JS，学有余力再上 TS）

---

> 进度：第 1–6 章已完成 ✅（环境、Git、前端三件套、Vue、Flask 后端 CRUD、MySQL 持久化）。下一步：第 7 章（前后端打通）。
