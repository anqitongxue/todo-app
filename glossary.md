# 术语表（GLOSSARY）

> 记录项目中遇到的专业名词，用"大白话"解释，方便随时查阅。
>
> **更新规则**：每遇到一个不懂的新词，就把它加进来。最好用自己的话写解释，比抄定义更记得住。

---

## 一、网络与通信

- **HTTP**：浏览器和服务器之间"对话"的规矩（协议）。你访问网页时，浏览器按这个规矩向服务器要数据。
- **HTTPS**：加了"加密锁"的 HTTP，数据传输更安全（就是浏览器地址栏前面的小锁）。
- **URL**：网址，例如 `https://www.example.com`，用来在网络上找到某个资源。
- **API**：程序之间沟通的"接口"。后端把数据包装成一个接口，前端调用它拿数据。好比餐厅菜单：你按菜单点菜（发请求），后厨做菜（后端处理），给你上菜（返回结果）。
- **JSON**：一种数据格式，长这样 `{"name": "学英语", "done": false}`。前后端约定都用它来"说话"。
- **请求 / 响应（Request / Response）**：前端向服务器"要东西"叫请求；服务器"给结果"叫响应。
- **跨域（CORS）**：浏览器的一种安全限制——不同地址的前后端默认不能互相访问，需要后端"放行"。

---

## 二、前端（浏览器里能看到的）

- **前端（Frontend）**：用户直接看到、点到的部分，跑在浏览器里。本项目的前端用 Vue。
- **Vue**：前端框架，核心思想是"数据驱动视图"（响应式 + 组件化）。
- **Vite**：构建工具/脚手架，负责生成项目骨架、本地开发服务器、打包发布。
- **组件（Component）**：界面拆出的可复用小块，一个 `.vue` 文件就是一个组件。
- **响应式（Reactive）**：数据一变，界面自动跟着变，不用手动操作 DOM。
- **单文件组件（SFC）**：`.vue` 文件，一个文件同时包含 `<template>`（结构）、`<script>`（逻辑）、`<style>`（样式）。
- **挂载（mount）**：`createApp(App).mount('#app')` 把 Vue 应用"挂"到 HTML 的某个元素上。
- **指令（Directive）**：Vue 模板里 `v-` 开头的特殊属性，如 `v-model`、`v-for`、`v-if`，给元素加特殊行为。
- **ref**：Vue 里声明"响应式数据"的函数，`ref(初始值)`，在 script 里用 `.value` 访问、在模板里直接写变量名。
- **双向绑定（v-model）**：数据和输入框互相绑定，一边变另一边跟着变，一行替代"手动读值 + 手动清空"。
- **插值（{{ }}）**：模板里用双花括号显示数据，如 `{{ task }}`。
- **条件渲染（v-if）**：`v-if="条件"`，条件为真时才渲染这个元素，为假时从页面移除（如"列表为空时显示提示"）。
- **props**：父组件传给子组件的数据。父组件写 `:属性="值"`，子组件用 `defineProps` 接收。
- **emit**：子组件向父组件"发事件"的机制。用 `defineEmits` 声明、`emit('事件名', 参数)` 触发，父组件用 `@事件名` 监听。
- **单向数据流**：数据只能从父流向子（props）；子要改数据，必须用 emit 上报，由父来改。
- **作用域样式（scoped）**：`<style scoped>` 让样式只作用于当前组件、不泄露到别的组件；全局样式（如 body）放 `style.css`。
- **HTML**：网页的"骨架"，用标签写出内容和结构（标题、段落、列表、输入框等）。
- **CSS**：网页的"外观/衣服"，控制颜色、大小、布局、间距等样式。
- **盒模型（Box Model）**：每个 HTML 元素都是一个"盒子"，从里到外依次是 content（内容）→ padding（内边距）→ border（边框）→ margin（外边距）。
- **Flex 布局（Flexbox）**：CSS 的一维布局方式，容器 `display: flex` 让子元素横/竖排列，用 `justify-content`、`align-items` 控制对齐与分布。
- **选择器（Selector）**：CSS 里用来「选中要装饰谁」的规则，如选中所有 `<h1>` 或所有 `class="xxx"` 的元素。
- **类（class）**：给 HTML 标签起的「分类名」，可多个标签共用，CSS 用 `.类名` 批量选中它们。
- **JavaScript**：网页的"行为/大脑"，让页面能交互（点击、添加、删除、发请求）。
- **变量（Variable）**：给数据起的名字，方便反复使用，用 `let`（可改）或 `const`（不可改）声明。
- **数组（Array）**：用 `[]` 装一列数据，如 `["a", "b", "c"]`，数据项按顺序排。
- **对象（Object）**：用 `{}` 装"带名字"的数据，如 `{ name: "学JS", done: false }`。
- **函数（Function）**：一段可重复调用的代码块，用 `function` 定义。
- **控制台（Console）**：浏览器开发者工具里打印调试信息的地方，JS 用 `console.log()` 往里输出。
- **事件（Event）**：用户与页面的交互（点击、输入、鼠标移动等），JS 用 `addEventListener` 监听事件、做出响应。
- **键盘事件（keydown）**：用户按下键盘按键时触发的事件，可用 `event.key` 判断按下的是哪个键。
- **事件对象（event）**：监听函数收到的参数，携带事件的详细信息（如按了哪个键、点的是哪个元素）。
- **remove() / splice()**：`元素.remove()` 把页面元素删掉；`数组.splice(位置, 个数)` 从数组指定位置删元素。
- **标签（Tag）**：HTML 里用尖括号包起来的东西，如 `<h1>`、`<p>`。多数标签成对出现，一开一关圈住内容。
- **属性（Attribute）**：写在标签里的附加信息，如 `<input type="text">` 里的 `type="text"`。
- **DOM**：网页在浏览器里的"树状结构"，JavaScript 可以操作它来改页面内容。

---

## 三、后端（用户看不到、跑在服务器上的）

- **后端（Backend）**：跑在服务器上的部分，负责处理逻辑、读写数据库。本项目的后端用 Flask。
- **Flask**：Python 的 Web 框架，用来写后端接口、处理请求、返回数据。
- **路由（Route）**：后端里"某个网址对应某段处理代码"的规则。
- **接口（Endpoint）**：后端暴露出来的一个个"功能入口"，前端通过这些入口要数据。
- **HTTP 方法**：请求的"动作类型"：GET（查）、POST（增）、PUT（改）、DELETE（删）。
- **RESTful**：设计 API 的约定：URL 用名词表资源（如 `/tasks`），HTTP 方法表操作。
- **jsonify**：Flask 里把 Python 的列表/字典转成 JSON 返回的函数。
- **HTTP 状态码**：服务器返回的状态数字，如 200 成功、201 创建成功、400 客户端错误、404 找不到、500 服务器错误。
- **请求体（Body）**：请求里携带的数据（如 POST 发来的 JSON），后端用 `request.get_json()` 读取。
- **路径参数（Path Parameter）**：URL 里动态的一段，如 `/tasks/<int:id>` 里的 id，用来定位具体某个资源。

---

## 四、数据库

- **数据库（Database）**：专门存数据的地方，比用文件存更可靠、能快速查询。本项目用 MySQL。
- **表（Table）**：数据库里的一张"表格"，比如"任务表""用户表"。
- **行（Row）**：表里的一条记录（如一个任务）。
- **列（Column）**：表里的一个字段（如 id、name、done）。
- **主键（Primary Key）**：唯一标识一条记录的列，通常用自增的 id。
- **外键（Foreign Key）**：指向另一张表主键的列，用来关联两张表。
- **pymysql**：Python 连接 MySQL 的库（"翻译官"），用来执行 SQL、读写数据。
- **事务 / commit**：数据库的增删改默认先"暂存"，`commit()` 才真正写入；出错可回滚。
- **ORM**：对象关系映射，用 Python 对象操作数据库，框架自动转成 SQL（如 SQLAlchemy）。
- **requirements.txt**：记录 Python 项目依赖的清单，用 `pip install -r requirements.txt` 一键安装。
- **软删除（Soft Delete）**：删除时不是真的从数据库删数据，而是打一个"已删除"标记（如 `is_deleted=1`），好处是能恢复、保留历史。
- **时间戳（Timestamp）**：记录数据创建/更新时间，MySQL 里 `DEFAULT CURRENT_TIMESTAMP` 可插入时自动填当前时间。
- **SQL**：用来和数据库对话的语言（增、删、改、查）。
- **持久化（Persistence）**：把数据存下来，程序重启也不丢。
- **root 账号**：MySQL 的超级管理员账号，权限最高。后面用代码连数据库时，就是用它（或它创建的其他账号）登录。
- **本机 MySQL 端口**：这台电脑的 MySQL 配的是 **8080** 端口（默认本是 3306），命令行连要用 `-P 8080`，Python 连也要写 8080。

---

## 五、工具与工程

- **终端（Terminal）**：输入命令控制电脑的窗口（常说的"命令行"）。
- **服务（Service）**：Windows 上一种"常驻后台、开机自动运行"的程序，不像普通命令跑完就退出。MySQL 就是一个后台服务。
- **包管理器（pip / npm）**：帮你下载、管理别人写好的"库/包"的工具。
- **虚拟环境（venv）**：给每个项目一个独立的 Python 环境，库只装在这个环境里，避免不同项目之间依赖冲突。
- **环境变量（Environment Variable）**：存在电脑上的一些"配置值"（比如数据库密码），通常不直接写进代码里。
- **PATH**：环境变量里最常用的一种，是一串"文件夹路径"清单——终端运行命令时，会按这份清单去一个个文件夹里找程序。装软件时勾选"Add to PATH"，就是把它登记进清单。
- **Git**：记录代码每次改动的版本管理工具。
- **仓库（Repository）**：用 Git 管理的"项目文件夹"。
- **提交（Commit）**：Git 里"保存一次改动"的动作。
- **依赖（Dependency）**：项目用到的、别人写好的库。
- **暂存区（Staging Area）**：`git add` 之后、`git commit` 之前的"候车区"，用来选择这次要存哪些改动。
- **代理（Proxy）**：本地的"中转站"，帮你转发网络请求。访问 GitHub 这类网站时需要它。
- **远程仓库（Remote）**：放在网上（如 GitHub）的仓库副本，和本地仓库同步。`origin` 是默认给远程仓库起的代号。
- **换行符（LF / CRLF）**：表示"换行"的字符，Linux/Mac 用 LF、Windows 用 CRLF。Git 若自动转换会报 `LF will be replaced by CRLF` 警告，用 `core.autocrlf=false` 关掉即可。
- **编码（Encoding）**：字符在电脑里的存储方式，常见 UTF-8（通用）和 GBK（中文老编码）。两者混用会产生"乱码"。

---

> 提示：这里只是"种子"术语。随着学习推进，请把自己遇到的每个新词都补进来，最好写一句"你自己的理解"。
